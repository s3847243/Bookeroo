version: 2.1

jobs:
  build_test:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run:
          name: install dependencies
          command: |
            npm install
	  - run:
		  name: run unit tests
		  command: |
			npm test

workflows:
  build_and_test:
    jobs:
      - build_test